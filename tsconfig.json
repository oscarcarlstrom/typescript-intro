{
  "compilerOptions": {
    "noEmit": true, //We are using babel to handle transpiling
    "noImplicitAny": true //Throws an error whenever TS detects an implicit 'any' type
  },
  "include": ["src/ts-components"], //Check these files only
  "exclude": ["node_modules"] //What to exclude
}
